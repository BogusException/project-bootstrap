# Architecture

## 1. Overview

*Write 1-2 sentences: what does this system do, and what problem does it solve?*

(Replace this line with a plain-language description of the project.)

## 2. Components

*List or table of the major parts. Keep it to the pieces a new contributor needs to understand on day one.*

| Component | Responsibility | File or module |
|-----------|---------------|----------------|
| (example) main entry | Parses CLI args, dispatches tasks | `run.sh` / `main.py` |

## 3. Data flow

*Describe how data moves through the system from arrival to output. A simple arrow diagram or numbered steps both work.*

1. Input: (where data comes from)
2. Processing: (what transforms it)
3. Output: (where results go)

## 4. External dependencies

*APIs, services, or databases this project calls at runtime. Link to their docs where useful.*

| Dependency | Type | Purpose |
|------------|------|---------|
| (example) | REST API | Fetches pricing data |
