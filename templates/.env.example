# Copy this file to .env and fill in real values.
# NEVER commit .env to git — it is blocked by .gitignore and the scan-secrets hook.
# Variable names use ALL_CAPS_SNAKE_CASE.
# Leave unused keys blank rather than deleting them.

# ── LLM APIs ──────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# ── Project-specific variables ────────────────────────────────────────────────
# Add below as the project evolves
